package Server.Controller;

import Client.Model.User;
import Server.Model.Connection;

import java.util.HashMap;

public class ConnectionHandler {
    private HashMap<User, Connection> connectionHashMap = new HashMap<>();
}
